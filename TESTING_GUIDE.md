# 🧪 دليل الاختبار - شيتاتي

## اختبار المشاكل التي تم حلها

### 1️⃣ اختبار عرض الموقع على الموبايل

#### خطوات الاختبار:
1. افتح الموقع `index.html` في المتصفح
2. افتح Developer Tools (اضغط F12)
3. فعّل وضع الموبايل (Device Toolbar - اضغط Ctrl+Shift+M)
4. جرب الأحجام التالية:
   - iPhone SE (375px)
   - iPhone 12 Pro (390px)
   - Samsung Galaxy S20 (360px)
   - iPad Mini (768px)

#### ما يجب التحقق منه:
- ✅ أزرار اختيار الدور (طالب/مساعد/دكتور) منظمة ولا تتداخل
- ✅ النصوص واضحة وقابلة للقراءة
- ✅ على الشاشات الصغيرة جداً (<400px)، تختفي النصوص وتظهر الأيقونات فقط
- ✅ المسافات والـ padding مناسبة
- ✅ الأزرار لا تتجاوز حدود الشاشة

#### صورة توضيحية:
![Mobile Improvements](../mobile_improvements_guide.png)

---

### 2️⃣ اختبار عدم تكرار مادة "اقتصاد 1"

#### خطوات الاختبار:

**السيناريو الأول - موقع جديد:**
1. امسح localStorage من المتصفح:
   ```javascript
   // افتح Console واكتب:
   localStorage.clear();
   sessionStorage.clear();
   ```
2. أعد تحميل الصفحة (F5)
3. اختر دورك (طالب/مساعد/دكتور)
4. **النتيجة المتوقعة:** تظهر مادة "اقتصاد 1" مرة واحدة فقط

**السيناريو الثاني - عدة تحديثات:**
1. أعد تحميل الصفحة عدة مرات (F5 × 5)
2. **النتيجة المتوقعة:** لا تزال مادة "اقتصاد 1" موجودة مرة واحدة فقط

**السيناريو الثالث - إغلاق وفتح المتصفح:**
1. أغلق المتصفح بالكامل
2. افتح الموقع مرة أخرى
3. **النتيجة المتوقعة:** لا تزال مادة "اقتصاد 1" موجودة مرة واحدة فقط

---

### 3️⃣ اختبار الأدوار المختلفة

#### دور الطالب 👨‍🎓:
1. اختر "طالب" من modal الترحيب
2. **يجب أن:**
   - ✅ لا يظهر زر "➕ إضافة مادة"
   - ✅ لا يظهر زر "🗑️ حذف المادة" عند فتح تفاصيل المادة
   - ✅ يمكنك مشاهدة وتحميل المواد فقط

#### دور المساعد 👨‍💼:
1. اختر "مساعد" من Role Toggle في الـ header
2. **يجب أن:**
   - ✅ يظهر زر "➕ إضافة مادة"
   - ✅ لا يظهر زر "🗑️ حذف المادة" عند فتح تفاصيل المادة
   - ✅ يمكنك إضافة مواد جديدة

#### دور الدكتور 👨‍🏫:
1. اختر "دكتور" من Role Toggle
2. **يجب أن:**
   - ✅ يظهر زر "➕ إضافة مادة"
   - ✅ يظهر زر "🗑️ حذف المادة" عند فتح تفاصيل المادة
   - ✅ يمكنك إضافة وحذف المواد

---

### 4️⃣ اختبار البحث 🔍

1. اكتب في صندوق البحث: "اقتصاد"
2. **يجب أن:** تظهر مادة "اقتصاد 1" فقط
3. امسح البحث
4. **يجب أن:** تظهر جميع المواد المتاحة

---

### 5️⃣ اختبار إضافة مادة جديدة (دكتور/مساعد فقط)

1. اضغط على "➕ إضافة مادة"
2. املأ النموذج:
   - اسم المادة: "محاسبة 1"
   - اسم الدكتور: "د. أحمد محمد"
   - ارفع ملف على File.io والصق الرابط
   - اختر صورة خلفية (اختياري)
3. اضغط "نشر الآن 🚀"
4. **يجب أن:**
   - ✅ تظهر رسالة نجاح
   - ✅ تظهر المادة الجديدة في الصفحة الرئيسية
   - ✅ يمكن فتحها ومشاهدة تفاصيلها

---

### 6️⃣ اختبار حذف مادة (دكتور فقط)

1. افتح تفاصيل أي مادة
2. اضغط "🗑️ حذف المادة"
3. أدخل كود الحذف: `mk990`
4. اضغط "🗑️ حذف المادة"
5. **يجب أن:**
   - ✅ تظهر رسالة تأكيد
   - ✅ تختفي المادة من الصفحة الرئيسية

---

### 7️⃣ اختبار الـ Dark Mode 🌙☀️

1. اضغط على أيقونة الـ Theme Toggle (🌙 أو ☀️)
2. **يجب أن:**
   - ✅ يتغير المظهر من فاتح لداكن أو العكس
   - ✅ تبقى الإعدادات محفوظة حتى بعد إعادة تحميل الصفحة

---

## 🐛 الإبلاغ عن Bugs

إذا وجدت أي مشكلة، يرجى:
1. فتح Developer Console (F12)
2. البحث عن أي أخطاء (Errors باللون الأحمر)
3. توثيق الخطوات التي أدت للمشكلة
4. أخذ screenshot إن أمكن

---

## ✅ Checklist سريع

- [ ] الموقع يعمل على الموبايل بشكل صحيح
- [ ] لا يوجد تكرار في مادة "اقتصاد 1"
- [ ] الأدوار (طالب/مساعد/دكتور) تعمل بشكل صحيح
- [ ] البحث يعمل
- [ ] إضافة المواد يعمل (للدكتور والمساعد)
- [ ] حذف المواد يعمل (للدكتور فقط)
- [ ] Dark Mode يعمل

---

## 📱 أحجام الشاشات المدعومة

- ✅ Mobile (320px - 768px)
- ✅ Tablet (768px - 1024px)
- ✅ Desktop (1024px+)
